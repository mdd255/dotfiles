# Kitty config — mirrored from .wezterm.lua
# ============================================

# Font
font_family      FiraCode Nerd Font Mono
font_size        12.8
modify_font cell_height 90%

# Colors (from WezTerm "AdventureTime" overrides)
background       #000000
cursor           #6cb6eb
cursor_text_color white
selection_background #6cb6eb
selection_foreground white

# Scrollback
scrollback_lines 10000

# Window
window_padding_width 0
confirm_os_window_close 0
tab_bar_style hidden

# Disable all default keybindings
clear_all_shortcuts yes

# Keybindings
map ctrl+r        set_tab_title
map alt+v         paste_from_clipboard
# Alt+P (command palette) — no Kitty equivalent; omitted
map ctrl+f        show_scrollback
map ctrl+t        new_tab_with_cwd
map ctrl+q        close_tab
map ctrl+n        scroll_line_down 15
map ctrl+e        scroll_line_up 15
map ctrl+v        copy_mode
map ctrl+super+f  toggle_fullscreen
map ctrl+tab      next_tab
map ctrl+shift+tab previous_tab
